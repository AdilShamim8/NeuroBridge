# v0.1.0 Pre-Release Checklist

Date: 2026-03-23

## Verification Status

- [x] All tests passing: `pytest tests/` (120 passed)
- [ ] Coverage >= 90%: current `85.19%` (fails Day 24 threshold)
- [ ] Type checking: `mypy neurobridge/` (fails)
- [ ] Linting: `ruff check neurobridge/` (fails)
- [ ] Black formatting: `black --check neurobridge/` (fails)
- [x] Package builds: `python -m build` produced `.tar.gz` and `.whl`
- [x] Twine metadata check: `python -m twine check dist/*` passed
- [x] Package installs cleanly: wheel install succeeded
- [x] Basic import works: `from neurobridge import NeuroBridge, Profile`
- [ ] All 5 profiles produce different output from same input (observed 4 unique outputs)
- [x] REST API server starts (`neurobridge serve` startup logs verified)
- [x] CLI commands work (`neurobridge info`, `neurobridge quiz`, `neurobridge adapt "test"`)
- [x] `CHANGELOG.md` updated
- [x] `README.md` PyPI badge references `v0.1.0`
- [x] License year is current (2026)

## Blocking Issues Before Public Release

1. Raise coverage from `85.19%` to at least `90%`.
2. Resolve mypy errors:
   - missing typed stubs for `nltk.tokenize`
   - syntax-level check issue surfaced during mypy run in test environment
3. Resolve ruff unused-import findings.
4. Resolve black formatting drift (`15 files would be reformatted`).
5. Ensure all five profile outputs differ for canonical release test input.

## Package Artifacts Generated

- `dist/neurobridge-0.1.0-py3-none-any.whl`
- `dist/neurobridge-0.1.0.tar.gz`

## TestPyPI and PyPI Commands

Run once release blockers are cleared and credentials are available:

```bash
python -m build
python -m twine upload --repository testpypi dist/*
python -m twine upload dist/*
```

Current environment upload readiness:
- TestPyPI upload attempt failed: missing API token credential
- PyPI upload attempt failed: missing API token credential

## Notes

- Twine upload requires configured credentials or API token.
- Website lint/build checks are pending in this environment because `npm` is unavailable.
